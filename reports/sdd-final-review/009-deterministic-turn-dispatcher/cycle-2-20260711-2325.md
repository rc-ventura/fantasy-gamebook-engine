# SDD Final Review — 009-deterministic-turn-dispatcher
**Cycle:** 2 (targeted re-verification, QA + Security only, per cycle 1's dispatch) | **Date:** 2026-07-11 23:25 | **Status:** APPROVED

---

## Security Engineer Report — Cycle 2 Re-Verification

### Findings
None remaining. The cycle-1 HIGH finding (unbounded classifier-controlled `enemies`/`flee_allowed` reaching `start_combat` when `combat` dispatches without a matching authored encounter) is **closed**.

### Adversarial checks performed

**1. Guard placement/order-of-operations (graph.py):** The guard at `graph.py:350-356` sits immediately after `params`/`params_source` are computed and before any code path that touches `run_check_step`/`start_combat`. Unconditional — no confidence threshold, no bypass flag. Checked for a stale/cross-zone id collision: the encounter list is scoped to `current_location` *before* matching by id, so an `action` string matching an encounter id authored for a *different* zone cannot produce a false-positive `encounter is not None`. No bypass found.

**2. Encounter-path isolation (check_engine.py):** Confirmed the trusted, author-controlled path (`encounter is not None`) uses `dict(encounter.params)` directly and never touches `clamp_params` — the new `bool`/`list` branches only affect the untrusted fallback path, exactly as intended.

**3. Test execution:** `uv run pytest tests/server/test_dispatcher.py -k TestCombatEncounterGuard -v` → 2 passed.

**4. Regression-proof (guard temporarily disabled):** Backed up `graph.py`, disabled the guard (`if False and ...`), reran — `test_combat_with_no_matching_encounter_falls_back_to_narrative` **FAILED** as expected (surfaced as a `ModelRetry` since `clamp_params`'s list-type fix forces `enemies=[]`, and the engine's own "at least one enemy" rule then rejects the call — confirms the two fixes are defense-in-depth, not redundant no-ops, and the guard is what turns this into a clean fallback rather than a raised exception). Restored `graph.py`, diffed byte-for-byte against the pre-edit backup ("Files are identical"), reran — both tests passed again. Full `test_dispatcher.py` suite: 38 passed.

### Verdict: PASS
**Confidence:** HIGH — **Finding CLOSED.** No new issues found.

---

## QA Engineer Report — Cycle 2 Re-Verification

**1. Test suite:** `uv run pytest tests/engine tests/server tests/qa -q` → **342 passed, 0 failed, 80 skipped** (up from 340). Ran twice for stability.

**2. OTLP production guard — restored correctly, still lacks an automated test:** Manually exercised both branches:
```
ENV=production OTLP_ENDPOINT=http://collector:4318  → RuntimeError raised ✅
ENV=production OTLP_ENDPOINT=https://collector:4318 → no error, proceeds ✅
```
Logic correct, docstring and comment fixed. No automated test covers this (`test_otel_instrumentation.py`/`test_observability.py`) — flagged as **non-blocking** (manually verified sound) with a recommended follow-up test (`monkeypatch.setenv` + `pytest.raises(RuntimeError)`).

**3. Bug #1 (move fallback matches `choice` not `choice_label`) — confirmed still present, untouched, correctly non-blocking:** `graph.py:365-374` unchanged from cycle 1 — the primary fix (classifier prompt instruction) is what actually carries button-click moves; this is a coverage gap in the defense-in-depth fallback, not an exploitable hole.

**Process note (not a bug):** Mid-review, QA transiently observed Security's in-progress edit (`if False and ...`, part of Security's own regression-proof step) and correctly identified it as concurrent-edit noise rather than a shipped regression, confirmed via direct execution that the on-disk guard fires correctly.

### Verdict: PASS
**Confidence:** HIGH
**Cycle 1 blocking items:** OTLP guard restoration — **closed**, verified by direct execution.
**Still open, non-blocking:** Bug #1 (`choice_label` gap in move fallback); new minor item — no automated test for the OTLP production guard (manually verified instead).

---

## Final Verdict: APPROVED

All four cycle-1 blocking items are closed and independently re-verified adversarially by both reviewers:
1. ✅ Combat guard (`encounter is not None` required) — closed, proven via disable/re-enable test.
2. ✅ `clamp_params` bounds `list`/`bool` — closed, confirmed not to affect the trusted encounter path.
3. ✅ Regression test (`TestCombatEncounterGuard`, 2 cases) — proven meaningful (fails without the fix, passes with it).
4. ✅ OTLP production TLS guard restored — closed, manually verified both branches.

Suite: **342 passed, 0 failed, 80 skipped**.

**Remaining non-blocking items** (tracked, not gating merge):
- `choice_label` gap in the `move` fallback (defense-in-depth coverage only; primary fix unaffected).
- Missing automated test for the OTLP production guard (logic manually verified sound).
- Orphaned `.claude/skills/ignarok/backbone.yaml` + `CONTRACTS.md` §16 location mismatch (cycle 1, Low).
- Follow-up ADR generalizing "every mechanical template needs an unconditional, classifier-independent authorization guard" (cycle 1 Tech Leader recommendation) — not blocking, tracked as future work alongside ADR-035.

Spec 009 is clear to commit and open a PR against `dev`.
